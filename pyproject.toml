[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "macrotrading"
version = "0.1.0"
description = "Risk-first macro theme evidence and alert engine"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
macrotrading = "macrotrading.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

