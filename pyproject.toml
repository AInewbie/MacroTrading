[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "macrotrading"
version = "0.6.0"
description = "Integrated macro research, portfolio risk and paper decision workbench"
requires-python = ">=3.11"
dependencies = []
[project.scripts]
macrotrading = "macrotrading.cli:main"
[tool.setuptools.packages.find]
where = ["src"]
